/// I changed `print` into a native function accepting multiple arguments. Set
/// this to true to use the standard Lox print statement instead.
pub const USE_PRINT_STMT = false;

pub const DEBUG_PRINT_CODE = false;
pub const DEBUG_TRACE_EXECUTION = false;
pub const DEBUG_STRESS_GC = false;
pub const DEBUG_LOG_GC = false;
